version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aurakomik-web
    ports:
      - "3333:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
